import { gameUpdate } from "../shared/Message";

function animate(update: gameUpdate) {
  const { meGod, mePlayer, otherGods, otherPlayers, blocks, ships, planets, meteors, cannonBalls, explosions, grapples } = update;
}
class PlayerAnimation {

}

class animationTicker {

}
